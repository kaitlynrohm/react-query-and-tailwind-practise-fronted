import { SafeAreaView } from "react-native";
import UserDisplay from "@/components/mine/UserDisplay";

export default function HomeScreen() {
  return (
    <SafeAreaView>
      <UserDisplay />
    </SafeAreaView>
  );
}
